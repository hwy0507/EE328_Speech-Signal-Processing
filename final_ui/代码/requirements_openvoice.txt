# Optional OpenVoice environment
# Recommended Python: 3.10
# Install OpenVoice itself separately if it is not available on pip in your setup.

numpy>=1.22.0,<2.0
scipy>=1.8.0
soundfile>=0.12.1
librosa>=0.10.0
pydub>=0.25.1
tqdm>=4.66.0
requests>=2.31.0

# OpenVoice common runtime dependencies
torch
torchaudio
transformers
unidecode
eng_to_ipa
inflect
wavmark

# If using a local OpenVoice repository, run for example:
# pip install -e path\to\OpenVoice
